name: Operator K8s E2E

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  e2e-k8s:
    name: Operator K8s E2E
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5

      - uses: docker/setup-buildx-action@v3

      # ── Create kind cluster ───────────────────────────────────────────────────
      - uses: helm/kind-action@v1
        with:
          config: packages/operator/uat/kind-cluster.yaml
          cluster_name: agentspec

      # ── Install cert-manager ──────────────────────────────────────────────────
      - name: Install cert-manager
        run: |
          kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.16.3/cert-manager.yaml
          kubectl wait --for=condition=Available --timeout=120s -n cert-manager deployment/cert-manager
          kubectl wait --for=condition=Available --timeout=120s -n cert-manager deployment/cert-manager-webhook
          kubectl wait --for=condition=Available --timeout=120s -n cert-manager deployment/cert-manager-cainjector
          # Allow cert-manager webhook to begin serving before helm runs
          sleep 15

      # ── Build + load operator image ───────────────────────────────────────────
      - name: Build operator image
        run: docker build -t agentspec-operator:ci -f packages/operator/Dockerfile packages/operator

      - name: Load operator image into kind
        run: kind load docker-image agentspec-operator:ci --name agentspec

      # ── Build + load sidecar image ────────────────────────────────────────────
      - name: Build sidecar image
        run: docker build -t agentspec-sidecar:ci -f packages/sidecar/Dockerfile .

      - name: Load sidecar image into kind
        run: |
          kind load docker-image agentspec-sidecar:ci --name agentspec
          # Also tag as agentspec/sidecar:local to match the demo deployment manifests
          docker tag agentspec-sidecar:ci agentspec/sidecar:local
          kind load docker-image agentspec/sidecar:local --name agentspec

      # ── Pre-load Helm hook image ──────────────────────────────────────────────
      - name: Pre-load check-cert-manager image into kind
        run: |
          docker pull bitnami/kubectl:1.31
          kind load docker-image bitnami/kubectl:1.31 --name agentspec

      # ── Helm install operator ─────────────────────────────────────────────────
      - name: Helm install agentspec-operator
        run: |
          helm upgrade --install agentspec-operator \
            packages/operator/helm/agentspec-operator \
            --namespace agentspec-system --create-namespace \
            --set operator.image.repository=agentspec-operator \
            --set operator.image.tag=ci \
            --set operator.image.pullPolicy=Never \
            --set webhook.sidecarImage=agentspec-sidecar:ci \
            --set webhook.enabled=true

      - name: Wait for webhook TLS certificate
        run: kubectl wait --for=condition=Ready certificate/agentspec-operator-webhook-cert -n agentspec-system --timeout=120s

      - name: Wait for operator deployment
        run: kubectl rollout status deployment/agentspec-operator -n agentspec-system --timeout=120s

      # ── Deploy gymcoach demo agent ────────────────────────────────────────────
      - name: Create demo namespace
        run: kubectl create namespace demo --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy gymcoach
        run: kubectl apply -k packages/operator/demo/gymcoach/

      - name: Wait for gymcoach rollout
        run: kubectl rollout status deployment/gymcoach --namespace demo --timeout=90s

      # ── Run E2E tests ─────────────────────────────────────────────────────────
      - name: Run operator E2E tests
        run: make operator-e2e

      # ── Debug artifacts on failure ────────────────────────────────────────────
      - name: Collect debug info
        if: always()
        run: |
          echo "=== Nodes ===" && kubectl get nodes -o wide || true
          echo "=== All pods ===" && kubectl get pods -A -o wide > /tmp/pods.txt && cat /tmp/pods.txt || true
          echo "=== Images loaded in kind ===" && docker exec agentspec-control-plane crictl images || true
          echo "=== Operator pod describe ===" && kubectl describe pods -n agentspec-system -l app.kubernetes.io/name=agentspec-operator > /tmp/operator-describe.txt 2>/dev/null || true && cat /tmp/operator-describe.txt || true
          echo "=== Operator logs (current) ===" && kubectl logs -n agentspec-system -l app.kubernetes.io/name=agentspec-operator --tail=100 > /tmp/operator.log 2>/dev/null || true && cat /tmp/operator.log || true
          echo "=== Operator logs (previous crash) ===" && kubectl logs -n agentspec-system -l app.kubernetes.io/name=agentspec-operator --previous --tail=100 > /tmp/operator-prev.log 2>/dev/null || true && cat /tmp/operator-prev.log || true
          echo "=== cert-manager events ===" && kubectl get events -n cert-manager --sort-by='.lastTimestamp' | tail -20 || true
          echo "=== agentspec-system events ===" && kubectl get events -n agentspec-system --sort-by='.lastTimestamp' | tail -20 || true
          echo "=== Certificates ===" && kubectl get certificates -A || true
          kubectl get agentobservations -A -o yaml > /tmp/agentobservations.yaml 2>/dev/null || true
          kubectl logs -n demo deployment/gymcoach -c sidecar --tail=100 > /tmp/sidecar.log 2>/dev/null || true

      - name: Upload debug artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-k8s-debug
          path: |
            /tmp/agentobservations.yaml
            /tmp/pods.txt
            /tmp/sidecar.log
            /tmp/operator.log
            /tmp/operator-describe.txt
            /tmp/operator-prev.log
          retention-days: 3
